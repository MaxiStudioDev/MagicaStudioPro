<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Magica Studio Pro</title>
  <style>
    body {
      background: linear-gradient(135deg, #0f172a, #1e293b);
      color: #fff;
      font-family: "Poppins", Arial, sans-serif;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      height: 100vh;
      margin: 0;
      text-align: center;
    }
    h1 { font-size: 3rem; margin-bottom: 0.5em; }
    p { color: #cbd5e1; max-width: 600px; margin-bottom: 2em; line-height: 1.5; }
    button {
      background: #3b82f6;
      color: #fff;
      border: none;
      padding: 15px 40px;
      border-radius: 12px;
      font-size: 18px;
      cursor: pointer;
      transition: all 0.2s ease;
      box-shadow: 0 4px 15px rgba(59, 130, 246, 0.4);
    }
    button:hover {
      background: #2563eb;
      transform: scale(1.05);
      box-shadow: 0 6px 20px rgba(37, 99, 235, 0.5);
    }
    footer { position: absolute; bottom: 15px; font-size: 0.9rem; color: #94a3b8; }
  </style>
</head>
<body>
  <h1>🚀 Magica Studio Pro</h1>
  <p>Télécharge directement <strong>MagicaSetup.exe</strong> 👇</p>

  <button id="downloadBtn">Télécharger maintenant 📦</button>

  <footer>© 2025 MaxiStudioDev — Tous droits réservés</footer>

  <script>
    const btn = document.getElementById("downloadBtn");

    // URL directe du fichier .exe
    const latestVersion = "1.0"; // tu peux changer automatiquement si tu veux
    const exeUrl = `https://github.com/MaxiStudioDev/MagicaStudioPro/releases/download/${latestVersion}/MagicaSetup.exe`;

    btn.onclick = () => {
      // Téléchargement direct
      window.location.href = exeUrl;
    };
  </script>
</body>
</html>
